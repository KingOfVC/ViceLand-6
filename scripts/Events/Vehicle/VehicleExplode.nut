/*SqCore.On().VehicleExplode.Connect( this, function( vehicle )
{
	local getVeh = SqVehicles.Vehicles[ vehicle.Tag ];
	
	if( getVeh.Prop.Name == "VL-Admin-69" )
	{
		vehicle.Destroy();

		SqVehicles.Vehicles.rawdelete( vehicle.Tag );
	}
});*/