SqCore.On().PlayerRename.Connect( this, function( player, old, new )
{
/*	if( player.Data.Logged )
	{
		SqCast.MsgAllExp( player, TextColor.Info, Lang.PlayerRename, old, TextColor.Info, new );

		SqCast.EchoMessage( format( "**%s** is now known as **%s**.", old, new ) );
	}*/
});